package com.wingedsheep.engine.core

/**
 * Engine-wide feature flags. Currently empty — left in place as a plumbing
 * point for future in-progress migrations.
 */
data class EngineFeatures(
    val unused: Unit = Unit,
)
