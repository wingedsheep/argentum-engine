/**
 * Placeholder for global click handling.
 * Card clicks are handled by Card3D component.
 */

/**
 * Global click handler for the 3D scene.
 *
 * Handles clicks that don't hit any card (to deselect).
 * Card-specific clicks are handled by the Card3D component.
 */
export function ClickHandler() {
  // Note: In R3F, we typically use onPointerMissed on the Canvas
  // This component is kept for potential future use with raycasting
  return null
}

