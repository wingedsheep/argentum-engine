// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.woe.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.references.Player
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Obyra, Dreaming Duelist
 * {U}{B}
 * Legendary Creature — Faerie Warrior
 * 2/2
 * Flash
 * Flying
 * Whenever another Faerie you control enters, each opponent loses 1 life.
 */
val ObyraDreamingDuelist = card("Obyra, Dreaming Duelist") {
    manaCost = "{U}{B}"
    colorIdentity = "UB"
    typeLine = "Legendary Creature — Faerie Warrior"
    oracleText = "Flash\nFlying\nWhenever another Faerie you control enters, each opponent loses 1 life."
    power = 2
    toughness = 2
    keywords(Keyword.FLASH, Keyword.FLYING)
    triggeredAbility {
        trigger = Triggers.entersBattlefield(
            // A bare tribal noun names *permanents* of that tribe, not creatures of it — the
            // reading the Assay differential settled across the corpus.
            filter = GameObjectFilter.Permanent.withSubtype(Subtype.FAERIE).youControl(),
            binding = TriggerBinding.OTHER
        )
        effect = Effects.LoseLife(1, EffectTarget.PlayerRef(Player.EachOpponent))
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "210"
        artist = "Evyn Fong"
        flavorText = "Obyra sleepwalks through the wilds, hunting for worthy opponents."
        imageUri = "https://cards.scryfall.io/normal/front/6/3/63ce03ee-279b-4955-98e3-9ce1990f8b7b.jpg"
    }
}
