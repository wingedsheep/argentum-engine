// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.ulg.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.ForEachTargetEffect
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Peace and Quiet
 * {1}{W}
 * Instant
 * Destroy two target enchantments.
 */
val PeaceAndQuiet = card("Peace and Quiet") {
    manaCost = "{1}{W}"
    colorIdentity = "W"
    typeLine = "Instant"
    oracleText = "Destroy two target enchantments."
    spell {
        val t = target("target", TargetPermanent(count = 2, filter = TargetFilter.Enchantment))
        effect = ForEachTargetEffect(
            listOf(Effects.Move(EffectTarget.ContextTarget(0), Zone.GRAVEYARD, byDestruction = true))
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "17"
        artist = "Don Hazeltine"
        flavorText = "\"In time our realm will shine again. But it will gleam only when we scour away the taint of doubt.\"\n—Radiant, archangel"
        imageUri = "https://cards.scryfall.io/normal/front/5/d/5d73accc-8f19-44d4-8216-c1acdbef3856.jpg"
    }
}
