// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mrd.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Lumengrid Sentinel
 * {2}{U}
 * Creature — Human Wizard
 * 1/2
 * Flying
 * Whenever an artifact you control enters, you may tap target permanent.
 */
val LumengridSentinel = card("Lumengrid Sentinel") {
    manaCost = "{2}{U}"
    colorIdentity = "U"
    typeLine = "Creature — Human Wizard"
    oracleText = "Flying\nWhenever an artifact you control enters, you may tap target permanent."
    power = 1
    toughness = 2
    keywords(Keyword.FLYING)
    triggeredAbility {
        trigger = Triggers.entersBattlefield(
            filter = GameObjectFilter.Artifact.youControl(),
            binding = TriggerBinding.ANY
        )
        optional = true
        val t = target("target", TargetPermanent())
        effect = Effects.Tap(t)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "40"
        artist = "Scott M. Fischer"
        flavorText = "The vedalken order their Neurok sentinels to watch over the shores of the Quicksilver Sea, as if they know of intruders yet to come."
        imageUri = "https://cards.scryfall.io/normal/front/4/8/48068b80-9a98-45d0-8224-ee298cbf708f.jpg"
    }
}
