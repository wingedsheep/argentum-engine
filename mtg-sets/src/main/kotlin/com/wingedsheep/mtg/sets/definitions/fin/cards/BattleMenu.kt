// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.fin.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.GainLifeEffect
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Battle Menu
 * {1}{W}
 * Instant
 * Choose one —
 * • Attack — Create a 2/2 white Knight creature token.
 * • Ability — Target creature gets +0/+4 until end of turn.
 * • Magic — Destroy target creature with power 4 or greater.
 * • Item — You gain 4 life.
 */
val BattleMenu = card("Battle Menu") {
    manaCost = "{1}{W}"
    colorIdentity = "W"
    typeLine = "Instant"
    oracleText = "Choose one —\n• Attack — Create a 2/2 white Knight creature token.\n• Ability — Target creature gets +0/+4 until end of turn.\n• Magic — Destroy target creature with power 4 or greater.\n• Item — You gain 4 life."
    spell {
        modal(chooseCount = 1) {
            mode("Attack — Create a 2/2 white Knight creature token") {
                effect = Effects.CreateToken(
                    power = 2,
                    toughness = 2,
                    colors = setOf(Color.WHITE),
                    creatureTypes = setOf("Knight")
                )
            }
            mode("Ability — Target creature gets +0/+4 until end of turn") {
                val t = target("target", TargetCreature(filter = TargetFilter.Creature))
                effect = Effects.ModifyStats(0, 4, t)
            }
            mode("Magic — Destroy target creature with power 4 or greater") {
                val t = target("target", TargetCreature(filter = TargetFilter.Creature.powerAtLeast(4)))
                effect = Effects.Move(t, Zone.GRAVEYARD, byDestruction = true)
            }
            mode("Item — You gain 4 life") {
                effect = GainLifeEffect(4)
            }
        }
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "9"
        artist = "Mingchen Shen"
        imageUri = "https://cards.scryfall.io/normal/front/2/4/240e1466-bd02-423d-b829-234dcd2bfab2.jpg"
    }
}
