// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mrd.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Deconstruct
 * {2}{G}
 * Sorcery
 * Destroy target artifact. Add {G}{G}{G}.
 */
val Deconstruct = card("Deconstruct") {
    manaCost = "{2}{G}"
    colorIdentity = "G"
    typeLine = "Sorcery"
    oracleText = "Destroy target artifact. Add {G}{G}{G}."
    spell {
        val t = target("target", TargetPermanent(filter = TargetFilter.Artifact))
        effect = Effects.Composite(
            Effects.Move(t, Zone.GRAVEYARD, byDestruction = true),
            Effects.AddMana(Color.GREEN, 3)
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "118"
        artist = "D. Alexander Gregory"
        flavorText = "\"Nothing in this world, not even magic, likes to be caged.\""
        imageUri = "https://cards.scryfall.io/normal/front/5/3/53face79-d2bf-43dd-aa4e-d724c2b68c49.jpg"
    }
}
