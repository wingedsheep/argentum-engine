// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.fin.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.EntersTapped
import com.wingedsheep.sdk.scripting.TimingRule


/**
 * Guadosalam, Farplane Gateway
 * Land — Town
 * This land enters tapped.
 * {T}: Add {G} or {U}.
 */
val GuadosalamFarplaneGateway = card("Guadosalam, Farplane Gateway") {
    manaCost = ""
    colorIdentity = "UG"
    typeLine = "Land — Town"
    oracleText = "This land enters tapped.\n{T}: Add {G} or {U}."
    replacementEffect(EntersTapped())
    activatedAbility {
        cost = Costs.Tap
        effect = Effects.AddMana(Color.GREEN)
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    activatedAbility {
        cost = Costs.Tap
        effect = Effects.AddMana(Color.BLUE)
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "281"
        artist = "Vaigintas Pakenis"
        flavorText = "\"We Guado protect the Farplane so the dead can rest in peace.\"\n—Guadosalam citizen"
        imageUri = "https://cards.scryfall.io/normal/front/d/f/dfcbc131-fd50-4908-b539-c8e52bb70c58.jpg"
    }
}
