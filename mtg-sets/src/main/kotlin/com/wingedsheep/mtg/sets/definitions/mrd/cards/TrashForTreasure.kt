// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mrd.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetObject


/**
 * Trash for Treasure
 * {2}{R}
 * Sorcery
 * As an additional cost to cast this spell, sacrifice an artifact.
 * Return target artifact card from your graveyard to the battlefield.
 */
val TrashForTreasure = card("Trash for Treasure") {
    manaCost = "{2}{R}"
    colorIdentity = "R"
    typeLine = "Sorcery"
    oracleText = "As an additional cost to cast this spell, sacrifice an artifact.\nReturn target artifact card from your graveyard to the battlefield."
    additionalCost(Costs.additional.SacrificePermanent(GameObjectFilter.Artifact))
    spell {
        val t = target(
            "target",
            TargetObject(filter = TargetFilter(GameObjectFilter.Artifact.ownedByYou(), zone = Zone.GRAVEYARD))
        )
        effect = Effects.Move(t, Zone.BATTLEFIELD)
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "109"
        artist = "Lars Grant-West"
        flavorText = "Goblins generally have two possessions: one per hand."
        imageUri = "https://cards.scryfall.io/normal/front/e/c/ec59ed99-ce40-489a-b776-8874bec60393.jpg"
    }
}
