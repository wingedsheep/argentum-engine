// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.dsk.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.filters.unified.GroupFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Split Up
 * {1}{W}{W}
 * Sorcery
 * Choose one —
 * • Destroy all tapped creatures.
 * • Destroy all untapped creatures.
 */
val SplitUp = card("Split Up") {
    manaCost = "{1}{W}{W}"
    colorIdentity = "W"
    typeLine = "Sorcery"
    oracleText = "Choose one —\n• Destroy all tapped creatures.\n• Destroy all untapped creatures."
    spell {
        modal(chooseCount = 1) {
            mode("Destroy all tapped creatures") {
                effect = Effects.ForEachInGroup(
                    GroupFilter(GameObjectFilter.Creature.tapped()),
                    Effects.Move(EffectTarget.Self, Zone.GRAVEYARD, byDestruction = true),
                    noRegenerate = false
                )
            }
            mode("Destroy all untapped creatures") {
                effect = Effects.ForEachInGroup(
                    GroupFilter(GameObjectFilter.Creature.untapped()),
                    Effects.Move(EffectTarget.Self, Zone.GRAVEYARD, byDestruction = true),
                    noRegenerate = false
                )
            }
        }
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "32"
        artist = "Dominik Mayer"
        flavorText = "\"Don't be a wimp. We'll cover more ground this way.\"\n—Rian, former survivor"
        imageUri = "https://cards.scryfall.io/normal/front/1/b/1bb9a5b1-48d2-453e-8991-c788bc63e482.jpg"
    }
}
