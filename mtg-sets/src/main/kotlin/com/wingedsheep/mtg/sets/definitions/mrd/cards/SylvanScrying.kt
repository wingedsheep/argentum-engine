// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mrd.cards

import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.SearchDestination


/**
 * Sylvan Scrying
 * {1}{G}
 * Sorcery
 * Search your library for a land card, reveal it, put it into your hand, then shuffle.
 */
val SylvanScrying = card("Sylvan Scrying") {
    manaCost = "{1}{G}"
    colorIdentity = "G"
    typeLine = "Sorcery"
    oracleText = "Search your library for a land card, reveal it, put it into your hand, then shuffle."
    spell {
        effect = Patterns.Library.searchLibrary(
            filter = GameObjectFilter.Land,
            destination = SearchDestination.HAND,
            reveal = true
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "130"
        artist = "Scott M. Fischer"
        flavorText = "One glimpse of an elf's home lasts her weeks away in the wild."
        imageUri = "https://cards.scryfall.io/normal/front/b/4/b4ff07b6-be9f-498f-9f36-cbd64f1b10cc.jpg"
    }
}
