// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.fin.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.TriggeredAbility
import com.wingedsheep.sdk.scripting.effects.CreateTokenEffect
import com.wingedsheep.sdk.scripting.effects.DealDamageEffect
import com.wingedsheep.sdk.scripting.references.Player
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Mysidian Elder
 * {2}{R}
 * Creature — Human Wizard
 * 1/3
 * When this creature enters, create a 0/1 black Wizard creature token with "Whenever you cast a noncreature spell, this token deals 1 damage to each opponent."
 */
val MysidianElder = card("Mysidian Elder") {
    manaCost = "{2}{R}"
    colorIdentity = "R"
    typeLine = "Creature — Human Wizard"
    oracleText = "When this creature enters, create a 0/1 black Wizard creature token with \"Whenever you cast a noncreature spell, this token deals 1 damage to each opponent.\""
    power = 1
    toughness = 3
    triggeredAbility {
        trigger = Triggers.EntersBattlefield
        effect = CreateTokenEffect(
            power = 0,
            toughness = 1,
            colors = setOf(Color.BLACK),
            creatureTypes = setOf("Wizard"),
            triggeredAbilities = listOf(
                TriggeredAbility.create(
                    trigger = Triggers.YouCastNoncreature.event,
                    binding = Triggers.YouCastNoncreature.binding,
                    effect = DealDamageEffect(1, EffectTarget.PlayerRef(Player.EachOpponent))
                )
            )
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "145"
        artist = "David Astruga"
        flavorText = "\"Perhaps this is a test and you must prove yourself worthy.\""
        imageUri = "https://cards.scryfall.io/normal/front/6/b/6bc39af4-be19-4889-b930-df7ebf7b9481.jpg"
    }
}
