// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.fdn.cards

import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.filters.unified.GroupFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget
import com.wingedsheep.sdk.scripting.targets.TargetOpponent


/**
 * Seeker's Folly
 * {2}{B}
 * Sorcery
 * Choose one —
 * • Target opponent discards two cards.
 * • Creatures your opponents control get -1/-1 until end of turn.
 */
val SeekersFolly = card("Seeker's Folly") {
    manaCost = "{2}{B}"
    colorIdentity = "B"
    typeLine = "Sorcery"
    oracleText = "Choose one —\n• Target opponent discards two cards.\n• Creatures your opponents control get -1/-1 until end of turn."
    spell {
        modal(chooseCount = 1) {
            mode("Target opponent discards two cards") {
                val t = target("target", TargetOpponent())
                effect = Patterns.Hand.discardCards(2, t)
            }
            mode("Creatures your opponents control get -1/-1 until end of turn") {
                effect = Effects.ForEachInGroup(
                    GroupFilter(GameObjectFilter.Creature.opponentControls()),
                    Effects.ModifyStats(-1, -1, EffectTarget.Self)
                )
            }
        }
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "69"
        artist = "Valera Lutfullina"
        flavorText = "While searching the foreboding woods for his favorite helmet, Syr Verlor lost his second-favorite helmet."
        imageUri = "https://cards.scryfall.io/normal/front/b/c/bc359da6-8b7f-45ec-b530-ce159fc35953.jpg"
    }
}
