// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.dst.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter


/**
 * Wand of the Elements
 * {4}
 * Artifact
 * {T}, Sacrifice an Island: Create a 2/2 blue Elemental creature token with flying.
 * {T}, Sacrifice a Mountain: Create a 3/3 red Elemental creature token.
 */
val WandOfTheElements = card("Wand of the Elements") {
    manaCost = "{4}"
    colorIdentity = ""
    typeLine = "Artifact"
    oracleText = "{T}, Sacrifice an Island: Create a 2/2 blue Elemental creature token with flying.\n{T}, Sacrifice a Mountain: Create a 3/3 red Elemental creature token."
    activatedAbility {
        cost = Costs.Composite(Costs.Tap, Costs.Sacrifice(GameObjectFilter.Land.withSubtype(Subtype.ISLAND)))
        effect = Effects.CreateToken(
            power = 2,
            toughness = 2,
            colors = setOf(Color.BLUE),
            creatureTypes = setOf("Elemental"),
            keywords = setOf(Keyword.FLYING)
        )
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Tap, Costs.Sacrifice(GameObjectFilter.Land.withSubtype(Subtype.MOUNTAIN)))
        effect = Effects.CreateToken(
            power = 3,
            toughness = 3,
            colors = setOf(Color.RED),
            creatureTypes = setOf("Elemental")
        )
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "158"
        artist = "Thomas M. Baxa"
        flavorText = "It gives legs to the earth and wings to the sky."
        imageUri = "https://cards.scryfall.io/normal/front/1/1/1131c187-8fc3-4cee-9422-355ef6622de7.jpg"
    }
}
