// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mmq.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.DealDamageEffect
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature
import com.wingedsheep.sdk.scripting.values.DynamicAmount


/**
 * Ballista Squad
 * {3}{W}
 * Creature — Human Rebel
 * 2/2
 * {X}{W}, {T}: This creature deals X damage to target attacking or blocking creature.
 */
val BallistaSquad = card("Ballista Squad") {
    manaCost = "{3}{W}"
    colorIdentity = "W"
    typeLine = "Creature — Human Rebel"
    oracleText = "{X}{W}, {T}: This creature deals X damage to target attacking or blocking creature."
    power = 2
    toughness = 2
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{X}{W}"), Costs.Tap)
        val t = target("target", TargetCreature(filter = TargetFilter.AttackingOrBlockingCreature))
        effect = DealDamageEffect(DynamicAmount.XValue, t)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "5"
        artist = "Matthew D. Wilson"
        flavorText = "The perfect antidote for a tightly packed formation."
        imageUri = "https://cards.scryfall.io/normal/front/3/0/30d51d84-23d2-41ff-ab68-a633beddba06.jpg"
    }
}
