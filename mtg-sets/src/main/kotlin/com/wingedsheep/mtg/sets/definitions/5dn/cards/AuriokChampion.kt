// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.`5dn`.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.KeywordAbility
import com.wingedsheep.sdk.scripting.ProtectionScope
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.effects.GainLifeEffect


/**
 * Auriok Champion
 * {W}{W}
 * Creature — Human Cleric
 * 1/1
 * Protection from black and from red
 * Whenever another creature enters, you may gain 1 life.
 */
val AuriokChampion = card("Auriok Champion") {
    manaCost = "{W}{W}"
    colorIdentity = "W"
    typeLine = "Creature — Human Cleric"
    oracleText = "Protection from black and from red\nWhenever another creature enters, you may gain 1 life."
    power = 1
    toughness = 1
    keywordAbility(KeywordAbility.Protection(ProtectionScope.Color(Color.BLACK)))
    keywordAbility(KeywordAbility.Protection(ProtectionScope.Color(Color.RED)))
    triggeredAbility {
        trigger = Triggers.entersBattlefield(filter = GameObjectFilter.Creature, binding = TriggerBinding.OTHER)
        optional = true
        effect = GainLifeEffect(1)
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "3"
        artist = "Michael Sutfin"
        flavorText = "\"Her will shall banish the shadows. Her might shall punish the lawless. Her beauty shall restore the light.\"\n—Ushanti, leonin seer"
        imageUri = "https://cards.scryfall.io/normal/front/b/9/b9e6bf0b-bb39-4d0f-9035-a276e604f609.jpg"
    }
}
