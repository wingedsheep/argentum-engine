// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.lci.cards

import com.wingedsheep.sdk.core.ManaCost
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.CounterCondition
import com.wingedsheep.sdk.scripting.effects.CounterEffect
import com.wingedsheep.sdk.scripting.targets.TargetSpell


/**
 * Confounding Riddle
 * {2}{U}
 * Instant
 * Choose one —
 * • Look at the top four cards of your library. Put one of them into your hand and the rest into your graveyard.
 * • Counter target spell unless its controller pays {4}.
 */
val ConfoundingRiddle = card("Confounding Riddle") {
    manaCost = "{2}{U}"
    colorIdentity = "U"
    typeLine = "Instant"
    oracleText = "Choose one —\n• Look at the top four cards of your library. Put one of them into your hand and the rest into your graveyard.\n• Counter target spell unless its controller pays {4}."
    spell {
        modal(chooseCount = 1) {
            mode(
                "Look at the top four cards of your library. Put one of them into your hand and the rest into your graveyard"
            ) {
                effect = Patterns.Library.lookAtTopAndKeep(count = 4, keepCount = 1)
            }
            mode("Counter target spell unless its controller pays {4}") {
                val t = target("target", TargetSpell())
                effect = CounterEffect(condition = CounterCondition.UnlessPaysMana(ManaCost.parse("{4}")))
            }
        }
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "50"
        artist = "A. M. Sartor"
        flavorText = "Explorers learned not to tamper with sites of ancient power without learning their purpose."
        imageUri = "https://cards.scryfall.io/normal/front/f/2/f2ae23db-c391-402c-9568-65447cada66e.jpg"
    }
}
