// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mrd.cards

import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.DrawCardsEffect
import com.wingedsheep.sdk.scripting.effects.SearchDestination


/**
 * Solemn Simulacrum
 * {4}
 * Artifact Creature — Golem
 * 2/2
 * When this creature enters, you may search your library for a basic land card, put that card onto the battlefield tapped, then shuffle.
 * When this creature dies, you may draw a card.
 */
val SolemnSimulacrum = card("Solemn Simulacrum") {
    manaCost = "{4}"
    colorIdentity = ""
    typeLine = "Artifact Creature — Golem"
    oracleText = "When this creature enters, you may search your library for a basic land card, put that card onto the battlefield tapped, then shuffle.\nWhen this creature dies, you may draw a card."
    power = 2
    toughness = 2
    triggeredAbility {
        trigger = Triggers.EntersBattlefield
        optional = true
        effect = Patterns.Library.searchLibrary(
            filter = GameObjectFilter.BasicLand,
            destination = SearchDestination.BATTLEFIELD,
            entersTapped = true
        )
    }
    triggeredAbility {
        trigger = Triggers.Dies
        optional = true
        effect = DrawCardsEffect(1)
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "245"
        artist = "Greg Staples"
        imageUri = "https://cards.scryfall.io/normal/front/0/0/00f9955f-a522-47bf-b064-92dd21a76b18.jpg"
    }
}
