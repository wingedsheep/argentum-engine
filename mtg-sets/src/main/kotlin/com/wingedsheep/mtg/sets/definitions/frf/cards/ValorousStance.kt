// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.frf.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Valorous Stance
 * {1}{W}
 * Instant
 * Choose one —
 * • Target creature gains indestructible until end of turn. (Damage and effects that say "destroy" don't destroy it.)
 * • Destroy target creature with toughness 4 or greater.
 */
val ValorousStance = card("Valorous Stance") {
    manaCost = "{1}{W}"
    colorIdentity = "W"
    typeLine = "Instant"
    oracleText = "Choose one —\n• Target creature gains indestructible until end of turn. (Damage and effects that say \"destroy\" don't destroy it.)\n• Destroy target creature with toughness 4 or greater."
    spell {
        modal(chooseCount = 1) {
            mode(
                "Target creature gains indestructible until end of turn. (Damage and effects that say \"destroy\" don't destroy it.)"
            ) {
                val t = target("target", TargetCreature(filter = TargetFilter.Creature))
                effect = Effects.GrantKeyword(Keyword.INDESTRUCTIBLE, t)
            }
            mode("Destroy target creature with toughness 4 or greater") {
                val t = target("target", TargetCreature(filter = TargetFilter.Creature.toughnessAtLeast(4)))
                effect = Effects.Move(t, Zone.GRAVEYARD, byDestruction = true)
            }
        }
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "28"
        artist = "Willian Murai"
        flavorText = "\"Every choice alters the path of fate.\"\n—Sarkhan Vol"
        imageUri = "https://cards.scryfall.io/normal/front/6/5/65998e94-15a0-41f1-8288-730b957f81df.jpg"
    }
}
