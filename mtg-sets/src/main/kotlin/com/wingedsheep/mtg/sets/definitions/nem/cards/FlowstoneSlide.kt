// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.nem.cards

import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.filters.unified.GroupFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget
import com.wingedsheep.sdk.scripting.values.DynamicAmount


/**
 * Flowstone Slide
 * {X}{2}{R}{R}
 * Sorcery
 * All creatures get +X/-X until end of turn.
 */
val FlowstoneSlide = card("Flowstone Slide") {
    manaCost = "{X}{2}{R}{R}"
    colorIdentity = "R"
    typeLine = "Sorcery"
    oracleText = "All creatures get +X/-X until end of turn."
    spell {
        effect = Effects.ForEachInGroup(
            GroupFilter(GameObjectFilter.Creature),
            Effects.ModifyStats(
                DynamicAmount.XValue,
                DynamicAmount.Multiply(DynamicAmount.XValue, -1),
                EffectTarget.Self
            )
        )
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "83"
        artist = "Chippy"
        flavorText = "It may look like soil, but its nature is malice."
        imageUri = "https://cards.scryfall.io/normal/front/e/c/ec7b02e1-0a20-4247-ae2a-056c5356f168.jpg"
    }
}
