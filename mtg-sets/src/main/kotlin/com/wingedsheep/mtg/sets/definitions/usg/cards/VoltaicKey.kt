// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.usg.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Voltaic Key
 * {1}
 * Artifact
 * {1}, {T}: Untap target artifact.
 */
val VoltaicKey = card("Voltaic Key") {
    manaCost = "{1}"
    colorIdentity = ""
    typeLine = "Artifact"
    oracleText = "{1}, {T}: Untap target artifact."
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{1}"), Costs.Tap)
        val t = target("target", TargetPermanent(filter = TargetFilter.Artifact))
        effect = Effects.Untap(t)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "314"
        artist = "Henry G. Higginbotham"
        flavorText = "The key did not work on a single lock, yet it opened many doors."
        imageUri = "https://cards.scryfall.io/normal/front/1/a/1aa4baf7-4693-4c55-af04-2fa5d901d701.jpg"
    }
}
