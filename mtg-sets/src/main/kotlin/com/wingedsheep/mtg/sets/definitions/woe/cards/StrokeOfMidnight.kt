// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.woe.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Stroke of Midnight
 * {2}{W}
 * Instant
 * Destroy target nonland permanent. Its controller creates a 1/1 white Human creature token.
 */
val StrokeOfMidnight = card("Stroke of Midnight") {
    manaCost = "{2}{W}"
    colorIdentity = "W"
    typeLine = "Instant"
    oracleText = "Destroy target nonland permanent. Its controller creates a 1/1 white Human creature token."
    spell {
        val t = target("target", TargetPermanent(filter = TargetFilter.NonlandPermanent))
        effect = Effects.Composite(
            Effects.Move(t, Zone.GRAVEYARD, byDestruction = true),
            woeHumanToken(controller = EffectTarget.TargetController)
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "33"
        artist = "Julia Metzger"
        flavorText = "The assembled partygoers fell silent and stared. As the helmet came to a clattering stop, the only sound was Ash's distant footsteps."
        imageUri = "https://cards.scryfall.io/normal/front/2/8/289ba7ec-e30e-436f-b8d4-c88b65ecd137.jpg"
    }
}
