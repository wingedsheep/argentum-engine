// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.fin.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.KeywordAbility
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.TriggeredAbility
import com.wingedsheep.sdk.scripting.effects.CreateTokenEffect
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Gysahl Greens
 * {1}{G}
 * Sorcery
 * Create a 2/2 green Bird creature token with "Whenever a land you control enters, this token gets +1/+0 until end of turn."
 * Flashback {6}{G} (You may cast this card from your graveyard for its flashback cost. Then exile it.)
 */
val GysahlGreens = card("Gysahl Greens") {
    manaCost = "{1}{G}"
    colorIdentity = "G"
    typeLine = "Sorcery"
    oracleText = "Create a 2/2 green Bird creature token with \"Whenever a land you control enters, this token gets +1/+0 until end of turn.\"\nFlashback {6}{G} (You may cast this card from your graveyard for its flashback cost. Then exile it.)"
    spell {
        effect = CreateTokenEffect(
            power = 2,
            toughness = 2,
            colors = setOf(Color.GREEN),
            creatureTypes = setOf("Bird"),
            triggeredAbilities = listOf(
                TriggeredAbility.create(
                    trigger = Triggers.entersBattlefield(
                        filter = GameObjectFilter.Land.youControl(),
                        binding = TriggerBinding.ANY
                    ).event,
                    binding = Triggers.entersBattlefield(
                        filter = GameObjectFilter.Land.youControl(),
                        binding = TriggerBinding.ANY
                    ).binding,
                    effect = Effects.ModifyStats(1, 0, EffectTarget.Self)
                )
            )
        )
    }
    keywordAbility(KeywordAbility.flashback("{6}{G}"))
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "190"
        artist = "Andrea Tentori Montalto"
        imageUri = "https://cards.scryfall.io/normal/front/0/2/020dee17-d85b-44c4-9faa-b1ef977956f4.jpg"
    }
}
